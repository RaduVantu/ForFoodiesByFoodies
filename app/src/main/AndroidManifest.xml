<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.forfoodiesbyfoodies">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ForFoodiesByFoodies">
        <activity android:name=".Register"></activity>
        <activity android:name=".AddShopReview" />
        <activity android:name=".ViewShopReview" />
        <activity android:name=".UserEdit" />
        <activity android:name=".UserProfile" />
        <activity android:name=".ShopDetail" />
        <activity android:name=".ShopList" />
        <activity android:name=".AddNewStall" />
        <activity android:name=".RestOrStall" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />

            </intent-filter>
        </activity>
    </application>
    <uses-permission android:name="android.permission.INTERNET" />
</manifest>